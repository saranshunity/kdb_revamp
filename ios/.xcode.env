# This `.xcode.env` file is versioned and is used to source the environment
# used when running script phases inside Xcode.
# To customize your local environment variables, you can create a `.xcode.env.local`
# file that is not versioned.

# NODE_BINARY variable contains the PATH to the node executable.
#
# Customize the NODE_BINARY variable here.
# For example, to use nvm with brew, add a line like:
# export NODE_BINARY=$(brew --prefix)/bin/node
# export NODE_BINARY=$(brew --prefix)/bin/node

# Enable new architecture
export RCT_NEW_ARCH_ENABLED=1
export RCT_FABRIC_ENABLED=1
export RCT_TURBO_MODULE_ENABLED=1